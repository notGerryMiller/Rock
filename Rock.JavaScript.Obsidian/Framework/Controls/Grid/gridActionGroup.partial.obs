<template>
    <div class="grid-actions">
        <GridActionButton v-for="action in props.gridActions" :action="action" />
    </div>
</template>

<script setup lang="ts">
    import { PropType } from "vue";
    import { GridAction } from "@Obsidian/Types/Controls/grid";
    import GridActionButton from "./gridActionButton.partial.obs";

    const props = defineProps({
        gridActions: {
            type: Array as PropType<GridAction[]>,
            default: []
        }
    });
</script>
